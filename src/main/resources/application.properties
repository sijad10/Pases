spring.application.name=pases
spring.datasource.url=jdbc:oracle:thin:@192.168.13.55:1521:renagi
spring.datasource.username=PASES
spring.datasource.password=PASES
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# ==============================================================
# =        CONFIGURACIÓN DE JPA / HIBERNATE                   =
# ==============================================================

# Mostrar las consultas SQL generadas por Hibernate (útil en desarrollo)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Codificación por defecto (recomendado)
spring.datasource.hikari.connection-test-query=SELECT 1 FROM DUAL